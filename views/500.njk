{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('500') }}</h1>
    <div>
        <p>{{ __('error_internal_server_error') }}</p>
    </div>

    {% if message %}
        <pre class="error">{{ message }}</pre>
    {% endif %}

{% endblock %}