<div class="page--container">
    <div class="fip-container">
        <div class="researchOps-logo">
            {# {% if getLocale() == 'fr' %}
                <img src="/img/sig-blk-fr.svg" alt="Gouvernement du Canada">
            {% else %} #}
                <img src="./img/Logo_Blue.svg" alt="Re+ Ops">
            {# {% endif %} #}
        </div>
        {% include "_includes/languageLink.njk" %}
    </div>
</div>