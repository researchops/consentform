{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('404') }}</h1>
    <div>
        <p>{{ __('error_page_not_found') }}</p>
    </div>

    {% if message %}
        <pre class="error">{{ message }}</pre>
    {% endif %}

{% endblock %}